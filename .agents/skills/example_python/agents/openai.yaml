interface:
  display_name: "Example Python"
  short_description: "Repo-specific patterns and workflows for example_python"
  default_prompt: "Use the example_python repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true